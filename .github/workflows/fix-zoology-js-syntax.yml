name: Fix Zoology test JavaScript syntax

on:
  push:
    paths:
      - '.github/workflows/fix-zoology-js-syntax.yml'

jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Repair malformed JavaScript separators
        shell: python
        run: |
          from pathlib import Path
          p = Path('zoology-chapter-test.html')
          s = p.read_text(encoding='utf-8')
          old = s
          s = s.replace(']\\', '],')
          s = s.replace('],n', '],')
          if s == old:
              print('No malformed JavaScript separators found')
          else:
              p.write_text(s, encoding='utf-8')
              print('Fixed malformed separators')
      - name: Commit fix
        shell: bash
        run: |
          if git diff --quiet -- zoology-chapter-test.html; then
            echo 'No changes needed'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add zoology-chapter-test.html
          git commit -m 'Fix remaining Zoology test JavaScript syntax'
          git push
